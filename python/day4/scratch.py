import regex

too_long_string = "The force of gravity between two objects is directly proportional to the product of their masses and inversely proportional to the square of the distance between their centers."
print(len(too_long_string))

approx_max_length_string = "For every action, there is an equal and opposite reaction."
print(len(approx_max_length_string))